"""Robot builders, state models, and runtime handles for MSSR simulations.

Submodules are intentionally not imported here. Isaac Sim extensions and the
graph/action stack have different initialization requirements, so eager package
imports make circular dependencies much more likely.
"""
